{
  "name": "pensando-em-css",
  "version": "1.0.0",
  "main": "index.md",
  "repository": "https://github.com/vhfmag/pensando-em-css",
  "author": "Victor Magalhães <victorhfmag@gmail.com>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@marp-team/marp-cli": "^0.16.2"
  },
  "scripts": {
    "generate-og-image": "marp --theme style.css marp --image png -o public/og.png --allow-local-files index.md",
    "serve": "marp --theme style.css --html -s .",
    "build": "yarn generate-og-image && marp --theme style.css --html index.md -o public/index.html --title 'Pensando em CSS' --url https://pensando-em-css.netlify.com --og-image og.png && cp -r images public/images"
  }
}
